package com.Java10;

public class RootCertificate_10 {

	// Now as Oracle has open sourced the root certificates using Oracle JAVA SE Root CA program, OpenJDK builds can now 
	// have root certificates and thus can reduce the difference between OpenJDK and Oracle JDK.
	
	
	//Oracle JAVA SE Root CA program issues the root certificates. Vendors who've signed the agreement, 
	//are included in the set of root certificates. The vendors who are not registered will be included in next release.
}
